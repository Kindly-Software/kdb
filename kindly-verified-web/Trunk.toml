# Trunk configuration for WASM build

[build]
target = "index.html"
dist = "dist"
release = true

[watch]
watch = ["src", "Cargo.toml"]
ignore = ["dist"]

[serve]
address = "127.0.0.1"
port = 8080
open = false
