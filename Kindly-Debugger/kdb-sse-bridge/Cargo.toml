[package]
name = "kdb-sse-bridge"
version = "1.0.0"
edition = "2021"
authors = ["Kindly Software Inc."]
description = "MCP stub for KDB debugger - advertises tools, use SSE for actual debugging"
publish = false

[workspace]

[dependencies]
# No dependencies - pure stdlib

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
